using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class tile_type_11 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        north = town;
        east = road;
        south = road;
        west = town;
        middle = town;
        blazon = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
