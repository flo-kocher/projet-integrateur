using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class tile_type_18 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        north = town;
        east = road;
        south = field;
        west = road;
        middle = road;
        blazon = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
