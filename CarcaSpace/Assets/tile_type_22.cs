using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class tile_type_22 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        north = field;
        east = road;
        south = road;
        west = road;
        middle = crossing;
        blazon = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
