using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class tile_type_13 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        north = field;
        east = town;
        south = field;
        west = town;
        middle = field;
        blazon = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
