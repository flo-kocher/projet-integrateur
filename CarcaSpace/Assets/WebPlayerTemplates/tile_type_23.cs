using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class tile_type_23 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        north = road;
        east = field;
        south = road;
        west = field;
        middle = crossing;
        blazon = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
