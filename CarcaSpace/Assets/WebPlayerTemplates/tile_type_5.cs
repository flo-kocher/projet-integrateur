using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class tile_type_5 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        north = town;
        east = town;
        south = road;
        west = town;
        middle = town;
        blazon = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
