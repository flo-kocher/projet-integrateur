using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class tile_type_20 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        north = field;
        east = field;
        south = road;
        west = field;
        middle = abbaye;
        blazon = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
