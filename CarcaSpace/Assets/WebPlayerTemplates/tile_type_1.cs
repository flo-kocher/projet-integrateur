using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class tile_type_1 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        north = town;
        east = town;
        south = town;
        west = town;
        middle = town;
        blazon = yes;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
