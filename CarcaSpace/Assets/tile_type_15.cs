using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class tile_type_15 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        north = town;
        east = road;
        south = road;
        west = field;
        middle = road;
        blazon = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
