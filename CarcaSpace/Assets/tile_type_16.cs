using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class tile_type_16 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        north = town;
        east = field;
        south = road;
        west = road;
        middle = road;
        blazon = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
