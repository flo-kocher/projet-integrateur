using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class tile_type_12 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        north = town;
        east = town;
        south = field;
        west = field;
        middle = field;
        blazon = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
