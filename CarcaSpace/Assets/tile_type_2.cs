using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class tile_type_2 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        north = town;
        east = town;
        south = field;
        west = town;
        middle = town;
        blazon = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
