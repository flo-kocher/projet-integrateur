using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class tile_type_24 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        north = field;
        east = field;
        south = road;
        west = road;
        middle = road;
        blazon = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
